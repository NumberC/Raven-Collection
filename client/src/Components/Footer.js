import { Component } from "react";
import "../CSS/Footer.css"

class Footer extends Component{
    render(){
        return(
            <footer>
            </footer>
        );
    }
}

export default Footer;